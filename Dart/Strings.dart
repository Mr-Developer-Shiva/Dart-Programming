void main() {
  String name = '   Shiva   '; //used ' ' or " " any one.
  print(name.length);
  print(name.isEmpty);
  print(name.toLowerCase());
  print(name.toUpperCase());
  print(name.trim());
  print(name.toString());
}
