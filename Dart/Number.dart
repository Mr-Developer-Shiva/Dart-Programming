void main() {
  num a = -1000;
  int x = 100;
  double z = 12.478474873;

  print(a.isNegative); //properties negative or positive...
  print(x.isEven); //integer values
  print(x.isOdd);
  print(x.toString()); // Covert to String
  print(x.toDouble()); //Convert to Double type
  print(z.toInt());     //  Convert to integer
  print(z.toStringAsFixed(4));      //multiple values to fix the digits
}
